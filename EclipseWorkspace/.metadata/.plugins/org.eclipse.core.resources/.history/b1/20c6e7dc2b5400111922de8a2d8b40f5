#include <stdio.h>
#include "sky_image.h"
int main()
{
    printf("Hello from Nios II!\n");
    int x = sky_image[1][1];
    printf("%d", x);
    while (1)
    {
        // keep running
    }

    return 0;
}
